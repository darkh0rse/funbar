machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker ps -a
    - docker images
    - df -h
    - mount
    - apt-get install curl
    - curl http://169.254.169.254/latest/meta-data/
